# 실제 구현 상태와 남은 작업

확인: 2026-09-19. 이 문서는 코드, 로컬 설정의 존재 여부, 실제 연결 검사에 근거한다. 키 값은 기록하지 않는다.

**점검 범위:** 아래 내용은 커밋 `1422637` 당시 프론트와 `runtime-infra/`, `nosana-connection/` 경로를 확인한 기록이다. 이후 원격 `main`의 `6f19537`에 별도 `backend/bench.py`와 `results_live.jsonl`이 추가됐다. 이 별도 Nosana 추론·Daytona 채점 경로의 실행 결과를 아래 런타임 경로의 검증 결과와 혼동하면 안 된다. 새로운 백엔드의 UI 연결은 병행 작업 중이며, 이 문서는 그 완료를 주장하지 않는다.

현재 제품은 **후보 생성 엔진 + 개별 실행 런타임 + 데모 UI** 단계다. 사용자가 넣은 워크로드를 모델로 실행하고 평가해 성능표에 표시하는 전체 흐름은 아직 완성되지 않았다. API 인증 성공과 실제 워크로드 벤치마크 성공을 구분해야 한다.

## 실행 전 성능표가 표시된 원인과 수정

- 초기 상태가 `demoSnapshot(...)`이어서, 사용자 실행 없이 15개 가상 점수와 지연 시간이 보였다. 데모 단계 전환도 실제 런타임 호출이 아니다.
- 초기 상태와 ‘새 벤치마크’는 이제 빈 결과다. 품질·시간은 ‘미측정’, 테스트는 ‘미실행’으로 표시한다.
- 수치가 있는 데모는 사용자가 명시적으로 ‘데모 미리보기’를 선택해야 나타난다. 데모 결과의 근거는 `illustrative`이며 `measured`로 받지 않는다.
- 실제 엔진의 후보 예측값은 성능표와 상세 성능 수치에 노출하지 않는다. 실측 전에는 구성 선택과 실측 추천을 제공하지 않는다.
- 실제 API 데이터는 실행이 끝난 `measured` 구성만 성능표에 표시한다. 최종 추천도 완료된 측정 결과에서만 선택한다.

## 서비스별 확인 결과

| 영역 | 확인된 내용 | 아직 필요한 작업 |
| --- | --- | --- |
| Daytona 인증·샌드박스 | 인증 성공. 실제 생성 → 파일 업로드 → Python 실행 → 결과 다운로드 → 삭제까지 smoke 통과 | 모델 실행 환경 준비 및 실제 모델 벤치마크 |
| Daytona 모델 런타임 | Python 실행기, Node 테스트 평가, 타임아웃, 순차 에이전트 실행, 정리 코드 구현 | GGUF 3종 확보, 이미지 빌드·등록, `DAYTONA_SNAPSHOT` 설정. 현재 실제 모델 추론은 미실행 |
| 후보·평가 엔진 | 후보 15개 생성, 상위 3개 사전 선별, 실측 결과를 받는 점수·추천 로직 | 실행 결과 수집·집계 연결. 사전 선별 수치는 보정되지 않은 예측이며 성능 근거가 아님 |
| Nosana | 별도 서버용 클라이언트의 인증 및 모델 목록 조회 성공 | 실제 워크로드 추론 경로와 평가 연결. 런타임의 GPU job 어댑터는 미구현 |
| DNSimple | sandbox 연결 확인용 코드 있음 | 토큰·계정 설정 및 실제 서비스 도메인 연결. 벤치마크의 선행 조건은 아님 |
| 파일 업로드 | 브라우저에서 텍스트 파일 읽기·검증 가능 | 내용을 실행 가능한 케이스·정답·테스트·평가 규칙으로 변환하고 검증하는 서버 처리 |
| 결과 보관 | 런타임에 최대 200개 작업을 담는 메모리 큐 | 영속 저장, 재시작 후 결과 조회, 원본 출력·실패·버전·해시 추적 |

Nosana 목록 조회 시 채팅 모델은 `qwen/qwen3.8-27b`, 임베딩 모델은 `nvidia/nemotron-3-embed-1b`가 반환됐다. 설계의 Qwen3 4B / DeepSeek R1 Distill Qwen 7B / Gemma 3 4B와 일치하지 않는다. API 키 입력만으로 이 세 모델을 Nosana에서 실행할 수 있다고 표시하면 안 된다. 제공 모델을 사용할지, 필요한 모델을 별도 GPU 작업으로 배포할지 먼저 결정하고 모델 ID와 실험 조건을 일치시켜야 한다. 이 점검에서 Nosana 유료 추론은 실행하지 않았다.

## 다음 구현 순서와 완료 기준

### 1. Daytona에서 실제 케이스 한 개를 끝까지 실행

1. `runtime-infra/README.md`에 지정된 GGUF 파일 3종과 양자화·버전을 고정한다. 이미지 빌드 가능한 Docker 환경이 필요하다.
2. `runtime-infra/infra/Dockerfile`로 llama-server와 모델을 포함한 이미지를 빌드하고 레지스트리에 올린다.
3. 이미지 digest로 스냅샷을 등록하고 `DAYTONA_SNAPSHOT`을 설정한다.
4. 런타임 서버를 실행하고, 모델 하나·코딩 케이스 하나로 실제 응답, Node 테스트 결과, 실행 시간, sandbox 정리를 검증한다.

완료 기준: 원본 모델 출력과 실제 평가 파일을 다운로드할 수 있고, 측정 결과가 해당 run ID와 연결되며 샌드박스 삭제가 확인된다. 42를 출력하는 연결 smoke는 이 완료 기준에 해당하지 않는다.

### 2. 실행 조건과 전체 워크로드 연결

현재 엔진의 compact/standard/extended 후보는 CPU·메모리·출력 토큰·타임아웃이 서로 다르다. 반면 런타임은 고정 정책과 스냅샷 자원을 검증한다. 현 상태 그대로 요청하면 조건 불일치로 거절될 수 있다. 동일 자원 비교인지, 자원까지 변화시키는 실험인지 정하고 엔진과 런타임의 정책을 맞춘다. 예측값을 실행 설정처럼 조용히 덮어쓰지 않는다.

- 상위 3개 구성 × 모든 워크로드 케이스의 실제 실행을 제출한다.
- 작업 상태를 polling하고 성공·실패·timeout을 모두 수집한다.
- 엔진의 aggregation/scoring 입력으로 변환하고, 전체 케이스가 확보된 실측 결과에만 추천을 적용한다.
- 모델 버전, architecture/workload hash, 시간 정의, 테스트 통과 수를 보존한다. 수집하지 않은 자원·비용은 0으로 만들지 않는다.

완료 기준: 같은 워크로드와 비교 정책으로 실행한 세 구성의 실측 성능·테스트 결과·추천을 API에서 조회할 수 있다.

### 3. UI 입력부터 실측 성능표까지 연결

프론트의 `/benchmarks`, `/benchmarks/:id/run`, `/benchmarks/:id`는 **제안된 계약**이며 기존 런타임·엔진 서버에 이 통합 API는 없다. `VITE_API_BASE_URL`만 채워도 해결되지 않는다.

- 업로드/설명을 케이스와 평가 기준으로 검증하는 통합 API를 만든다. 우선 지원 범위는 기본 코딩 워크로드로 고정해도 된다.
- 실행 시작 → 대기/실행/평가 → 실제 결과 → 추천을 UI와 연결한다.
- 결과·출력·평가 기록을 영속 저장하고 새로고침/서버 재시작 이후 조회를 지원한다.
- 개발 시 Vite 프록시, 배포 시 실제 백엔드 라우팅을 구성한다. 현재 개발 포트는 UI 5173, 엔진 3002, 런타임 3001이다. 런타임 3001은 이번 점검 시 실행 중이지 않았다.

완료 기준: 새 브라우저에서 워크로드 제출 후 실제 클라우드 run ID가 생성되고, 평가 완료 전 성능표는 비어 있으며, 완료 후 실제 결과만 표시되고 새로고침해도 유지된다.

### 4. Nosana와 DNSimple 확장

- Nosana: 사용할 모델/운영 방식을 확정하고 서버용 클라이언트 또는 GPU job 어댑터를 실행 인터페이스에 연결한다. 실제 모델 응답을 동일 평가기에 넘겨 별도 실험 조건으로 기록한다.
- DNSimple: 배포할 앱/서비스 주소와 도메인이 준비된 후 sandbox에서 검증하고 실제 도메인 라우팅·TLS를 구성한다. DNSimple은 모델을 실행하거나 성능을 측정하지 않는다.

## 이번 수정 검증

- 프론트 단위 테스트 12개 통과: 실제 엔진 계약, 파일 입력 검증, 빈 초기 상태, 가상/실측 근거 구분, 미실행 후보의 차트 제외.
- 추가 브라우저 테스트 3개 통과: 초기/데모/초기화, 실제 엔진 응답 형식의 후보 탐색, 모바일 빈 상태. 후보 탐색 테스트는 실제 엔진 함수를 사용한 응답 fixture로 실행한다.
- TypeScript 검사와 Vite production build 통과.
- 초기 화면과 후보 화면 screenshot 확인: `artifacts/benchmark-not-run.png`, `artifacts/candidates-not-measured.png`.
- 실행 중인 로컬 엔진(3002)에 직접 연결한 브라우저에서도 후보 15개와 빈 성능표, 미측정 상세, 비활성 선택 버튼 확인: `artifacts/engine-live-not-measured.png`.
- 이번 프론트 수정이 실제 모델 벤치마크 검증을 의미하지 않는다. 기존 영문 UI 기준의 전체 E2E는 이번에 실행하지 않았다.

상세 런타임 검증 범위: `runtime-infra/VALIDATION.md`. Nosana 연결 방식: `nosana-connection/README.md`.
