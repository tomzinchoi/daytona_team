# Optional complete orchestration service, not implemented by the current backend sessions.
# It must implement the validated Snapshot contract described in README.md.
# VITE_API_BASE_URL=https://your-orchestrator.example/api

# Local engine/runtime connections use Vite's /engine and /runtime proxies.
# Never put Daytona/provider secrets into VITE_ variables: they are public browser code.
