{
  "architecture": {
    "id": "qwen-single",
    "agents": [{ "model": "qwen3-4b", "role": "Solve the task and return only the requested JSON." }]
  },
  "benchmarkCase": {
    "id": "arithmetic-json",
    "task": "Return exactly a JSON object with key answer and numeric value equal to 6 multiplied by 7. No markdown or extra keys.",
    "evaluator": { "type": "json_exact", "expected": { "answer": 42 } }
  }
}
